<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Home Page</title>

    <link rel="stylesheet" href="/vendor/normalize-4.1.1.css" type="text/css" />
    <link rel="stylesheet" href="/vendor/border-box.css" type="text/css" />
    <link rel="stylesheet" href="/styles/main.css" type="text/css" />
    <link rel="stylesheet" href="/styles/layout.css" type="text/css" />

    <script type="text/javascript" src="/vendor/jquery-3.0.0.js"></script>
    <script src="/scripts/test.js"></script>


  </head>

  <body >
    </div>
    <div style="width:33%; margin-right:60px;">
      <div>
        <h1>Hello 👋, <%= user.user_name %></h1>
        <img src="<%= user.profile_photo %>" alt="profile photo">
      </div>

      <div>
        <h2>Your maps:</h2>
        <div id="test-map-widget">
          <% include test_map_widget %>
        </div>
      </div>
      <div>
        <h2>Here are some maps created by other users</h2>
        <% for(const map of otherMaps) { %>
          <form class="map-button" id="map_<%=map.map_id%>" action="/map/<%= map.map_id %>" method="get">
            <input map-id="<%=map.map_id%>" type="submit" value="<%= map.map_name %>"></input>
          </form>
        <% } %>
        </ul>
      </div>
      <div>
        <h2>Create new map</h2>
        <form id="new-map" user-id="<%= user.user_id%>" action="/map" method="post">
          <label for="map_name">Map Name</label>
          <input id="map_name"type="text" name="map_name">
          <input type="submit" value="Create">
        </form>
      </div>
    </div>

    <div style="width:33%">
      <div id="locations-list" >
        <% include test_locations %>
      </div>
      <div id="locations-form" style="display:block ;"><% include locations_form %></div>
      <div id="location-info"></div>
    </div>
  </body>
</html>
